#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, k;
        cin >> n >> k;
        int end;
        for(int i=0;i<n;i++){
            if(pow(2,i)>n) {
                end=i-1;
                break;
            }
        }
        while(1){
            
        }

    }
    return 0;
}